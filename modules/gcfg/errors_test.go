package gcfg_test

import "testing"

func TestXxx(t *testing.T) {
	tests := []struct {
		name string
		in   error
		want error
	}{}

	for _, tc := range tests {
		t.Run(tc.name, func(t *testing.T) {
			// For
		})
	}
}
